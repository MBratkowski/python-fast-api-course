---
phase: 06-production-part-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 017-docker-containers/README.md
  - 017-docker-containers/theory/01-container-concepts.md
  - 017-docker-containers/theory/02-dockerfile-basics.md
  - 017-docker-containers/theory/03-multi-stage-builds.md
  - 017-docker-containers/theory/04-docker-compose.md
  - 017-docker-containers/theory/05-networking-volumes.md
  - 017-docker-containers/theory/06-production-optimizations.md
  - 017-docker-containers/exercises/01_dockerfile.py
  - 017-docker-containers/exercises/02_docker_compose.py
  - 017-docker-containers/exercises/03_multi_stage_build.py
  - 017-docker-containers/project/README.md
  - 018-ci-cd-deployment/README.md
  - 018-ci-cd-deployment/theory/01-cicd-concepts.md
  - 018-ci-cd-deployment/theory/02-github-actions-basics.md
  - 018-ci-cd-deployment/theory/03-ci-testing-pipeline.md
  - 018-ci-cd-deployment/theory/04-docker-image-building.md
  - 018-ci-cd-deployment/theory/05-cloud-deployment.md
  - 018-ci-cd-deployment/theory/06-env-vars-secrets.md
  - 018-ci-cd-deployment/exercises/01_workflow_file.py
  - 018-ci-cd-deployment/exercises/02_test_pipeline.py
  - 018-ci-cd-deployment/exercises/03_deployment_config.py
  - 018-ci-cd-deployment/project/README.md
autonomous: true
requirements: [PROD-01, PROD-02]

must_haves:
  truths:
    - "Learner can understand container concepts and how they compare to mobile app packaging"
    - "Learner can write a Dockerfile for a FastAPI application with proper layer caching"
    - "Learner can configure Docker Compose for multi-container setups (API + PostgreSQL + Redis)"
    - "Learner can understand CI/CD pipeline concepts and how they compare to mobile CI (Xcode Cloud, Bitrise)"
    - "Learner can write GitHub Actions workflow files for testing and Docker image building"
    - "Learner can manage environment variables and secrets in CI/CD pipelines"
  artifacts:
    - path: "017-docker-containers/theory/01-container-concepts.md"
      provides: "Container fundamentals with mobile-dev analogies"
      contains: "Why This Matters"
    - path: "017-docker-containers/theory/03-multi-stage-builds.md"
      provides: "Multi-stage build patterns for smaller production images"
      contains: "multi-stage"
    - path: "017-docker-containers/exercises/01_dockerfile.py"
      provides: "Exercise: write and validate a Dockerfile"
      contains: "TODO"
    - path: "018-ci-cd-deployment/theory/02-github-actions-basics.md"
      provides: "GitHub Actions workflow syntax and concepts"
      contains: "Why This Matters"
    - path: "018-ci-cd-deployment/exercises/01_workflow_file.py"
      provides: "Exercise: write and validate a GitHub Actions workflow"
      contains: "TODO"
    - path: "018-ci-cd-deployment/project/README.md"
      provides: "Project: full CI/CD pipeline"
      contains: "Requirements"
  key_links:
    - from: "017-docker-containers/theory/02-dockerfile-basics.md"
      to: "017-docker-containers/exercises/01_dockerfile.py"
      via: "Theory teaches Dockerfile syntax; exercise applies it"
      pattern: "Dockerfile"
    - from: "017-docker-containers/theory/04-docker-compose.md"
      to: "017-docker-containers/exercises/02_docker_compose.py"
      via: "Theory teaches Compose; exercise validates compose config"
      pattern: "docker-compose"
    - from: "018-ci-cd-deployment/theory/02-github-actions-basics.md"
      to: "018-ci-cd-deployment/exercises/01_workflow_file.py"
      via: "Theory teaches Actions syntax; exercise validates workflow YAML"
      pattern: "github.actions"
---

<objective>
Create complete content for Module 017 (Docker and Containers) and Module 018 (CI/CD and Deployment).

Purpose: These modules teach learners how to containerize FastAPI applications and automate testing/deployment pipelines -- the first steps toward production readiness.

Output: 22 files total -- 6 theory files, 3 exercises, 1 project README, and 1 module README per module.
</objective>

<execution_context>
@/Users/mbratkowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbratkowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-production-part-a/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Module 017 Docker and Containers content</name>
  <files>
    017-docker-containers/README.md
    017-docker-containers/theory/01-container-concepts.md
    017-docker-containers/theory/02-dockerfile-basics.md
    017-docker-containers/theory/03-multi-stage-builds.md
    017-docker-containers/theory/04-docker-compose.md
    017-docker-containers/theory/05-networking-volumes.md
    017-docker-containers/theory/06-production-optimizations.md
    017-docker-containers/exercises/01_dockerfile.py
    017-docker-containers/exercises/02_docker_compose.py
    017-docker-containers/exercises/03_multi_stage_build.py
    017-docker-containers/project/README.md
  </files>
  <action>
    Create all Module 017 content following the established pattern from Phases 1-5.

    **Theory files (6):**
    Each theory file must include a "Why This Matters" section with mobile-dev analogies (Docker image = .ipa/.apk bundle, Dockerfile = Xcode scheme/build.gradle, Docker Compose = multi-target Xcode project) and "Key Takeaways" at the end. Include code examples throughout.

    1. `01-container-concepts.md` -- What containers are, containers vs VMs, why containers for backend, Docker architecture (images, containers, registry). Mobile analogy: container = app sandbox on iOS/Android.
    2. `02-dockerfile-basics.md` -- FROM, WORKDIR, COPY, RUN, CMD, EXPOSE. Layer caching strategy (copy requirements.txt first, then app code). Always use exec-form CMD `["fastapi", "run", ...]` not shell form. Build from `python:3.12-slim` (NOT tiangolo/uvicorn-gunicorn-fastapi which is deprecated).
    3. `03-multi-stage-builds.md` -- Two-stage pattern: builder stage installs deps, runtime stage copies only what's needed. Show size comparison. Non-root user creation with groupadd/useradd. Mobile analogy: debug vs release builds.
    4. `04-docker-compose.md` -- Multi-container orchestration with services, ports, environment, depends_on with healthcheck conditions. Show FastAPI + PostgreSQL + Redis compose file. Use Docker Compose v2 syntax (`docker compose` not `docker-compose`). Include volumes for data persistence.
    5. `05-networking-volumes.md` -- Docker networking (bridge, service discovery by name), named volumes vs bind mounts, volume persistence across container restarts. When to use each volume type.
    6. `06-production-optimizations.md` -- .dockerignore, minimizing layers, security scanning, health checks in Dockerfile, Gunicorn with Uvicorn workers for production, environment variable configuration.

    **Exercises (3):**
    These exercises are UNIQUE compared to prior modules. Since Docker exercises involve writing Dockerfiles/YAML (not Python code), use Python test files that READ and VALIDATE Dockerfile/docker-compose.yml content. Learners write the Docker config files; pytest tests verify correctness. Each exercise file should:
    - Include inline pytest tests that validate file content
    - Have TODO stubs where learners write the Docker/YAML content as Python string variables or as actual files that get parsed
    - Use `pass` or minimal code so tests fail until learner fills in stubs

    1. `01_dockerfile.py` -- Exercise: Write a Dockerfile for a FastAPI app. Provide a TODO stub where the learner defines Dockerfile content as a multi-line string. Tests validate: uses python:3.12-slim base, has WORKDIR, copies requirements.txt before app code (layer caching), uses exec-form CMD, exposes port 8000.
    2. `02_docker_compose.py` -- Exercise: Write a docker-compose.yml for FastAPI + PostgreSQL + Redis. TODO stub for compose YAML content. Tests validate: has 3 services, postgres has healthcheck, api depends_on db, environment variables are set, volumes defined.
    3. `03_multi_stage_build.py` -- Exercise: Write a multi-stage Dockerfile. TODO stub for Dockerfile content. Tests validate: has 2+ FROM statements (AS builder pattern), creates non-root user, COPY --from=builder is used, final stage uses slim image.

    **Project README:**
    "Containerize a Full API" -- Learner containerizes a FastAPI application with PostgreSQL and Redis using Docker Compose. Requirements: multi-stage Dockerfile, docker-compose.yml with health checks, .dockerignore, non-root user, environment variable configuration. Include starter template structure and success criteria.

    **Module README:**
    Overview of the module with learning objectives, prerequisites (Docker Desktop installed), and links to theory/exercises/project.
  </action>
  <verify>
    <automated>find 017-docker-containers/theory -name "*.md" | wc -l | grep -q "6" && find 017-docker-containers/exercises -name "*.py" | wc -l | grep -q "3" && grep -q "Why This Matters" 017-docker-containers/theory/01-container-concepts.md && grep -q "TODO" 017-docker-containers/exercises/01_dockerfile.py && echo "PASS"</automated>
  </verify>
  <done>Module 017 has 6 theory files with mobile-dev analogies, 3 exercise files with TODO stubs and inline pytest tests that validate Dockerfile/YAML content, 1 project README with containerization requirements, and 1 module README.</done>
</task>

<task type="auto">
  <name>Task 2: Create Module 018 CI/CD and Deployment content</name>
  <files>
    018-ci-cd-deployment/README.md
    018-ci-cd-deployment/theory/01-cicd-concepts.md
    018-ci-cd-deployment/theory/02-github-actions-basics.md
    018-ci-cd-deployment/theory/03-ci-testing-pipeline.md
    018-ci-cd-deployment/theory/04-docker-image-building.md
    018-ci-cd-deployment/theory/05-cloud-deployment.md
    018-ci-cd-deployment/theory/06-env-vars-secrets.md
    018-ci-cd-deployment/exercises/01_workflow_file.py
    018-ci-cd-deployment/exercises/02_test_pipeline.py
    018-ci-cd-deployment/exercises/03_deployment_config.py
    018-ci-cd-deployment/project/README.md
  </files>
  <action>
    Create all Module 018 content following the established pattern from Phases 1-5.

    **Theory files (6):**
    Each theory file must include "Why This Matters" with mobile-dev analogies (GitHub Actions = Xcode Cloud/Bitrise, CI test runner = XCTest/JUnit, deployment pipeline = TestFlight/Play Console upload) and "Key Takeaways."

    1. `01-cicd-concepts.md` -- What CI/CD is, continuous integration vs continuous delivery vs continuous deployment, why automate (catch bugs early, consistent builds), the pipeline concept (commit -> test -> build -> deploy). Mobile analogy: Xcode Cloud runs tests on every commit, Bitrise builds and distributes.
    2. `02-github-actions-basics.md` -- Workflow files (.github/workflows/), triggers (on push/pull_request), jobs and steps, runners (ubuntu-latest), actions marketplace (checkout, setup-python). YAML syntax with real examples.
    3. `03-ci-testing-pipeline.md` -- Setting up pytest in CI, service containers (PostgreSQL in GitHub Actions), caching pip dependencies, test matrix (multiple Python versions), test reporting. Show complete workflow that runs pytest with a PostgreSQL service container.
    4. `04-docker-image-building.md` -- Building Docker images in CI, docker/build-push-action, tagging with git SHA, pushing to registries (Docker Hub, GitHub Container Registry). Multi-platform builds. Caching Docker layers in CI.
    5. `05-cloud-deployment.md` -- Overview of deployment targets: Railway, Fly.io, AWS ECS, DigitalOcean App Platform. Show deployment pattern (build image -> push to registry -> deploy to platform). Environment-specific configuration. Blue-green and rolling deployments concepts.
    6. `06-env-vars-secrets.md` -- GitHub Actions secrets (${{ secrets.NAME }}), environment variables in workflows, .env files for local dev (python-dotenv), never commit secrets, secrets rotation. Mobile analogy: Keychain/EncryptedSharedPreferences = environment variables/secrets manager.

    **Exercises (3):**
    Like Module 017, these exercises validate YAML workflow files. Use Python exercises that parse and validate GitHub Actions YAML content. Learners define workflow YAML; pytest validates structure.

    1. `01_workflow_file.py` -- Exercise: Write a basic GitHub Actions workflow. TODO stub for workflow YAML content. Tests validate: has `on` trigger, has `jobs` section, uses `actions/checkout@v4`, uses `actions/setup-python@v5`, runs pytest.
    2. `02_test_pipeline.py` -- Exercise: Write a CI pipeline with PostgreSQL service container. TODO stub. Tests validate: has services section with postgres, health check options, environment variables for DATABASE_URL, pytest step with env vars.
    3. `03_deployment_config.py` -- Exercise: Write a workflow that builds and tags a Docker image. TODO stub. Tests validate: has build step, uses docker/build-push-action or docker build command, tags with github.sha, has proper job dependency (needs: test).

    **Project README:**
    "Full CI/CD Pipeline" -- Learner creates a complete CI/CD pipeline for a FastAPI project. Requirements: CI workflow that runs tests with PostgreSQL, Docker image build workflow triggered on main branch, proper secrets handling, deployment configuration for one platform (learner's choice). Include starter template and success criteria.

    **Module README:**
    Overview with learning objectives, prerequisites (GitHub account, basic Docker knowledge from Module 017), and links to theory/exercises/project.
  </action>
  <verify>
    <automated>find 018-ci-cd-deployment/theory -name "*.md" | wc -l | grep -q "6" && find 018-ci-cd-deployment/exercises -name "*.py" | wc -l | grep -q "3" && grep -q "Why This Matters" 018-ci-cd-deployment/theory/01-cicd-concepts.md && grep -q "TODO" 018-ci-cd-deployment/exercises/01_workflow_file.py && echo "PASS"</automated>
  </verify>
  <done>Module 018 has 6 theory files with mobile-dev analogies, 3 exercise files with TODO stubs and inline pytest tests that validate GitHub Actions YAML content, 1 project README with CI/CD pipeline requirements, and 1 module README.</done>
</task>

</tasks>

<verification>
- All 22 files exist in 017-docker-containers/ and 018-ci-cd-deployment/
- Every theory file contains "Why This Matters" and "Key Takeaways"
- Every exercise file contains TODO stubs and inline pytest tests
- Exercise tests validate Docker/YAML content (not Python application code)
- Project READMEs include requirements, starter templates, and success criteria
- No deprecated patterns used (no tiangolo image, no docker-compose v1, no shell-form CMD)
</verification>

<success_criteria>
- Module 017: 6 theory files covering containers through production optimizations, 3 exercises validating Dockerfile/Compose content, 1 project for full containerization
- Module 018: 6 theory files covering CI/CD through secrets management, 3 exercises validating GitHub Actions YAML, 1 project for complete pipeline
- All content uses mobile-dev analogies consistently
- Exercises follow the novel pattern of Python tests validating infrastructure config files
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-part-a/06-01-SUMMARY.md`
</output>
