---
phase: 09-complete-missing-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 018-ci-cd-deployment/theory/04-docker-image-building.md
  - 018-ci-cd-deployment/theory/05-cloud-deployment.md
  - 018-ci-cd-deployment/theory/06-environment-variables-secrets.md
  - 018-ci-cd-deployment/exercises/01_workflow_file.py
  - 018-ci-cd-deployment/exercises/02_ci_test_pipeline.py
  - 018-ci-cd-deployment/exercises/03_deployment_config.py
  - 018-ci-cd-deployment/project/README.md
autonomous: true
requirements:
  - PROD-02

must_haves:
  truths:
    - "Module 018 has 6 theory files covering CI/CD concepts through env vars and secrets"
    - "Module 018 has 3 exercises with TODO stubs and embedded pytest tests that fail until implemented"
    - "Module 018 has a project README with requirements, starter template, and success criteria"
  artifacts:
    - path: "018-ci-cd-deployment/theory/04-docker-image-building.md"
      provides: "Docker image building in CI theory"
      contains: "Why This Matters"
    - path: "018-ci-cd-deployment/theory/05-cloud-deployment.md"
      provides: "Cloud deployment patterns theory"
      contains: "Why This Matters"
    - path: "018-ci-cd-deployment/theory/06-environment-variables-secrets.md"
      provides: "Env vars and secrets management theory"
      contains: "Why This Matters"
    - path: "018-ci-cd-deployment/exercises/01_workflow_file.py"
      provides: "GitHub Actions workflow exercise"
      contains: "TODO"
    - path: "018-ci-cd-deployment/exercises/02_ci_test_pipeline.py"
      provides: "CI test pipeline exercise"
      contains: "TODO"
    - path: "018-ci-cd-deployment/exercises/03_deployment_config.py"
      provides: "Deployment config exercise"
      contains: "TODO"
    - path: "018-ci-cd-deployment/project/README.md"
      provides: "CI/CD pipeline project"
      contains: "Starter Template"
  key_links:
    - from: "018-ci-cd-deployment/theory/04-docker-image-building.md"
      to: "018-ci-cd-deployment/theory/01-cicd-concepts.md"
      via: "builds on CI/CD concepts from theory 01-03"
      pattern: "multi-stage|docker build"
    - from: "018-ci-cd-deployment/exercises/"
      to: "018-ci-cd-deployment/theory/"
      via: "exercises apply theory concepts"
      pattern: "pytest.*018-ci-cd-deployment"
---

<objective>
Complete Module 018 (CI/CD and Deployment) with remaining theory files, all exercises, and project README.

Purpose: Module 018 already has 3 of 6 theory files and a comprehensive README. This plan adds the remaining 3 theory files (Docker image building, cloud deployment, env vars/secrets), creates 3 exercises using the Python dict-to-YAML validation pattern, and adds the project README. This satisfies requirement PROD-02.

Output: 3 theory markdown files, 3 exercise Python files with embedded pytest tests, 1 project README.md
</objective>

<execution_context>
@/Users/mbratkowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbratkowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-complete-missing-content/09-RESEARCH.md

Style reference files (read these before writing to match tone and structure):
@018-ci-cd-deployment/theory/01-cicd-concepts.md
@018-ci-cd-deployment/theory/02-github-actions-basics.md
@018-ci-cd-deployment/theory/03-ci-testing-pipeline.md
@018-ci-cd-deployment/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Module 018 theory files 04-06</name>
  <files>
    018-ci-cd-deployment/theory/04-docker-image-building.md
    018-ci-cd-deployment/theory/05-cloud-deployment.md
    018-ci-cd-deployment/theory/06-environment-variables-secrets.md
  </files>
  <action>
    CRITICAL: Read the existing 3 theory files (01, 02, 03) in 018-ci-cd-deployment/theory/ FIRST and match their exact tone, structure, level of detail, code example density, and mobile analogy style.

    Create 3 theory files following the established pattern:

    **04-docker-image-building.md** (~150-200 lines):
    - "Why This Matters" with mobile analogy (Xcode archives to IPA vs Docker builds to images; Bitrise build steps vs GitHub Actions docker steps)
    - Multi-stage Docker builds for Python/FastAPI (builder stage with deps, runtime stage with app only)
    - GitHub Actions workflow for building and pushing to Docker Hub / GitHub Container Registry
    - Using docker/build-push-action@v5 and docker/login-action@v3
    - Tagging strategies (git SHA, semver, latest)
    - Build caching with actions/cache and Docker layer caching
    - Comparison table: mobile artifact build vs Docker image build
    - Key Takeaways (5-8 bullets)

    **05-cloud-deployment.md** (~150-200 lines):
    - "Why This Matters" with mobile analogy (App Store / Play Store review vs instant server deployment; TestFlight beta vs staging environments)
    - Three deployment platforms: Railway (simplest), Fly.io (more control), AWS ECS (production-grade)
    - For each: how it works, Dockerfile requirements, deployment command/config
    - GitHub Actions deployment workflow for Railway (using railway-deploy action or CLI)
    - Staging vs production environments
    - Health check endpoints for deployment verification
    - Zero-downtime deployment concepts (rolling updates)
    - Comparison table: mobile distribution vs backend deployment
    - Key Takeaways (5-8 bullets)

    **06-environment-variables-secrets.md** (~150-200 lines):
    - "Why This Matters" with mobile analogy (Xcode schemes with different bundle IDs / Android build flavors vs env-based config; Keychain / encrypted properties vs GitHub Secrets)
    - GitHub Secrets: creating, using in workflows with ${{ secrets.NAME }}
    - Environment-specific configs (dev, staging, production)
    - .env files locally vs secrets in CI (never commit .env)
    - Pydantic Settings for loading env vars in FastAPI (BaseSettings pattern)
    - Secret rotation strategies
    - Common mistakes: hardcoded secrets, logging secrets, committing .env
    - Comparison table: mobile secret management vs backend secret management
    - Key Takeaways (5-8 bullets)
  </action>
  <verify>
    <automated>ls 018-ci-cd-deployment/theory/*.md | wc -l | grep -q "6" && echo "PASS: 6 theory files" || echo "FAIL"</automated>
  </verify>
  <done>Module 018 theory/ directory contains 6 markdown files (01-06), each with "Why This Matters" mobile analogies, code examples, comparison tables, and "Key Takeaways" sections. New files 04-06 match the tone and structure of existing files 01-03.</done>
</task>

<task type="auto">
  <name>Task 2: Create Module 018 exercises and project</name>
  <files>
    018-ci-cd-deployment/exercises/01_workflow_file.py
    018-ci-cd-deployment/exercises/02_ci_test_pipeline.py
    018-ci-cd-deployment/exercises/03_deployment_config.py
    018-ci-cd-deployment/project/README.md
  </files>
  <action>
    Create 3 exercise files using the Python dict-to-YAML validation pattern (since CI/CD exercises are about YAML configuration, not executable Python). Follow the established exercise pattern: docstring with instructions and mobile analogy, TODO stubs with hints, embedded pytest tests.

    **01_workflow_file.py** (~120-180 lines):
    - Docstring: Write a basic GitHub Actions workflow for a Python project
    - Mobile analogy: Like defining your Bitrise/Xcode Cloud pipeline steps, but in YAML
    - Task: Implement `create_ci_workflow() -> dict` that returns a valid GitHub Actions workflow structure
    - Requirements: name "CI", triggers on push/PR to main, one "test" job on ubuntu-latest, steps for checkout (actions/checkout@v4), setup-python 3.12 (actions/setup-python@v5), install deps (pip install -r requirements.txt), run pytest
    - Tests: verify name, triggers, job exists, steps contain checkout/setup-python/install/test, python version is "3.12"
    - Optional: `import yaml` to verify round-trip serialization (but yaml is not required for tests to pass)

    **02_ci_test_pipeline.py** (~120-180 lines):
    - Docstring: Create a CI pipeline with PostgreSQL service container
    - Mobile analogy: Like configuring a test scheme with a specific simulator, but here you configure a database container
    - Task: Implement `create_test_pipeline_with_db() -> dict` that returns a workflow with PostgreSQL service container
    - Requirements: service container (postgres:16, env vars for POSTGRES_PASSWORD/POSTGRES_DB, port 5432, health check options), DATABASE_URL env var in test step pointing to localhost postgres
    - Tests: verify services block, postgres image, health check, DATABASE_URL pattern, port mapping

    **03_deployment_config.py** (~120-180 lines):
    - Docstring: Create a Docker image build and push workflow
    - Mobile analogy: Like configuring App Store Connect deployment in Xcode Cloud, but for Docker Hub
    - Task: Implement `create_docker_build_workflow() -> dict` that returns a workflow for building and pushing Docker images
    - Requirements: triggers on push to main only (not PR), job with steps for checkout, Docker login (docker/login-action@v3 with secrets.DOCKER_USERNAME and secrets.DOCKER_TOKEN), build and push (docker/build-push-action@v5 with push:true and tags)
    - Tests: verify trigger is push-only (no pull_request), Docker login step uses secrets, build-push step has push:true, tags include repository name

    **project/README.md** (~100-150 lines):
    - Follow the established project README pattern from other modules
    - Title: "Project: Complete CI/CD Pipeline"
    - Overview: Build a full CI/CD pipeline for a FastAPI application
    - Requirements sections: (1) CI Testing Pipeline -- workflow with postgres service, pytest, coverage reporting; (2) Docker Image Build -- multi-stage Dockerfile, build and push workflow, tagging strategy; (3) Deployment -- deploy to Railway or Fly.io, staging + production environments; (4) Secrets Management -- all secrets via GitHub Secrets, no hardcoded values
    - Starter Template: file structure (.github/workflows/ci.yml, .github/workflows/deploy.yml, Dockerfile, docker-compose.yml) with skeleton YAML
    - Success Criteria: 5-6 numbered verifiable outcomes
    - Stretch Goals: 3-4 optional extensions (matrix testing, Slack notifications, canary deploys)
  </action>
  <verify>
    <automated>ls 018-ci-cd-deployment/exercises/*.py | wc -l | grep -q "3" && ls 018-ci-cd-deployment/project/README.md > /dev/null 2>&1 && pytest 018-ci-cd-deployment/exercises/ -v --co 2>&1 | grep -q "test session starts" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Module 018 has 3 exercise Python files with TODO stubs and embedded pytest tests that collect without import errors, plus a project README with requirements, starter template, and success criteria. Running `pytest --co` on the exercises directory shows test collection succeeds.</done>
</task>

</tasks>

<verification>
Module 018 content verification:
1. `ls 018-ci-cd-deployment/theory/*.md | wc -l` returns 6
2. `ls 018-ci-cd-deployment/exercises/*.py | wc -l` returns 3
3. `ls 018-ci-cd-deployment/project/README.md` exists
4. `pytest 018-ci-cd-deployment/exercises/ -v --co` collects tests without import errors
5. Each theory file contains "Why This Matters" and "Key Takeaways" sections
6. Each exercise file contains "TODO" stubs and embedded test classes
</verification>

<success_criteria>
Module 018 is complete with 6 theory files, 3 exercises, and 1 project README. All content follows the established patterns from completed modules. Theory files 04-06 match the tone and style of existing files 01-03. Exercises use the dict-to-YAML validation pattern and all tests collect successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/09-complete-missing-content/09-01-SUMMARY.md`
</output>
