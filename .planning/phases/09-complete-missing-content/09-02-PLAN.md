---
phase: 09-complete-missing-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 020-performance-optimization/README.md
  - 020-performance-optimization/theory/01-profiling.md
  - 020-performance-optimization/theory/02-query-analysis.md
  - 020-performance-optimization/theory/03-n-plus-one-queries.md
  - 020-performance-optimization/theory/04-connection-pooling.md
  - 020-performance-optimization/theory/05-async-best-practices.md
  - 020-performance-optimization/theory/06-load-testing.md
  - 020-performance-optimization/exercises/01_profile_code.py
  - 020-performance-optimization/exercises/02_fix_n_plus_one.py
  - 020-performance-optimization/exercises/03_load_test.py
  - 020-performance-optimization/project/README.md
autonomous: true
requirements:
  - PROD-04

must_haves:
  truths:
    - "Module 020 has a comprehensive README matching the established module README pattern"
    - "Module 020 has 6 theory files covering profiling through load testing"
    - "Module 020 has 3 exercises with TODO stubs and embedded pytest tests that fail until implemented"
    - "Module 020 has a project README with requirements, starter template, and success criteria"
  artifacts:
    - path: "020-performance-optimization/README.md"
      provides: "Comprehensive module overview with mobile context table"
      contains: "Mobile Developer Context"
    - path: "020-performance-optimization/theory/01-profiling.md"
      provides: "Python profiling with cProfile theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/theory/02-query-analysis.md"
      provides: "Database query analysis theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/theory/03-n-plus-one-queries.md"
      provides: "N+1 query problem and solutions theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/theory/04-connection-pooling.md"
      provides: "SQLAlchemy connection pooling theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/theory/05-async-best-practices.md"
      provides: "Async optimization patterns theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/theory/06-load-testing.md"
      provides: "Load testing with Locust theory"
      contains: "Why This Matters"
    - path: "020-performance-optimization/exercises/01_profile_code.py"
      provides: "Profiling exercise with cProfile"
      contains: "TODO"
    - path: "020-performance-optimization/exercises/02_fix_n_plus_one.py"
      provides: "N+1 query fix exercise with SQLAlchemy + SQLite"
      contains: "TODO"
    - path: "020-performance-optimization/exercises/03_load_test.py"
      provides: "Load testing / benchmarking exercise"
      contains: "TODO"
    - path: "020-performance-optimization/project/README.md"
      provides: "Performance optimization project"
      contains: "Starter Template"
  key_links:
    - from: "020-performance-optimization/exercises/02_fix_n_plus_one.py"
      to: "020-performance-optimization/theory/03-n-plus-one-queries.md"
      via: "exercise applies N+1 theory with SQLAlchemy selectinload"
      pattern: "selectinload|joinedload"
    - from: "020-performance-optimization/exercises/"
      to: "020-performance-optimization/theory/"
      via: "exercises apply theory concepts"
      pattern: "pytest.*020-performance-optimization"
---

<objective>
Create all Module 020 (Performance Optimization) content from scratch: README rewrite, 6 theory files, 3 exercises, and project README.

Purpose: Module 020 currently has only a minimal README stub (63 lines). This plan creates all content from scratch -- a comprehensive README, 6 theory files covering profiling through load testing, 3 exercises using SQLite in-memory and pure Python patterns, and a project README. This satisfies requirement PROD-04.

Output: 1 rewritten README.md, 6 theory markdown files, 3 exercise Python files with embedded pytest tests, 1 project README.md
</objective>

<execution_context>
@/Users/mbratkowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbratkowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-complete-missing-content/09-RESEARCH.md

Style reference files (read for tone, structure, and README pattern):
@018-ci-cd-deployment/README.md
@021-logging-monitoring/theory/01-python-logging.md
@021-logging-monitoring/exercises/01_logging_config.py
@021-logging-monitoring/project/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Module 020 README and create all 6 theory files</name>
  <files>
    020-performance-optimization/README.md
    020-performance-optimization/theory/01-profiling.md
    020-performance-optimization/theory/02-query-analysis.md
    020-performance-optimization/theory/03-n-plus-one-queries.md
    020-performance-optimization/theory/04-connection-pooling.md
    020-performance-optimization/theory/05-async-best-practices.md
    020-performance-optimization/theory/06-load-testing.md
  </files>
  <action>
    FIRST: Read the Module 018 README.md as the comprehensive README pattern reference (110 lines with mobile context table, prerequisites, time estimates, example code). Also read 2-3 theory files from Module 021 (logging-monitoring) or Module 023 (rate-limiting) to calibrate tone and depth.

    **Rewrite 020-performance-optimization/README.md** (~100-120 lines):
    - Match the Module 018 README pattern exactly
    - "Why This Module?" with mobile analogy (Instruments/Xcode profiling, Android Profiler/LeakCanary vs cProfile/SQLAlchemy profiling)
    - "What You'll Learn" bullet list (6 items)
    - "Mobile Developer Context" with comparison table: iOS (Instruments, Core Data fetch optimization, URLSession config) vs Android (Android Profiler, Room query optimization, OkHttp connection pool) vs Python (cProfile, SQLAlchemy eager loading, connection pool config)
    - "Key Differences from Mobile Profiling" bullets
    - Prerequisites checklist
    - Topics section (Theory 1-6, Exercises 1-3, Project)
    - Time Estimate
    - Example code snippet (N+1 fix or profiling example)

    **Create 6 theory files** (~150-200 lines each):

    **01-profiling.md**:
    - "Why This Matters" with mobile analogy (Instruments Time Profiler / Android Studio CPU Profiler vs cProfile)
    - Python profiling tools: cProfile (stdlib), line_profiler, memory_profiler
    - Using cProfile: `python -m cProfile script.py`, programmatic usage with `cProfile.Profile()`
    - Reading profiler output: tottime, cumtime, ncalls
    - Visualizing with snakeviz (`snakeviz profile.prof`)
    - FastAPI middleware for request timing
    - Comparison table: mobile profiling tools vs Python profiling tools
    - Key Takeaways

    **02-query-analysis.md**:
    - "Why This Matters" with mobile analogy (Core Data fetch request optimization / Room @Query with EXPLAIN vs SQLAlchemy query analysis)
    - SQLAlchemy query logging: `echo=True` on engine
    - EXPLAIN ANALYZE in PostgreSQL
    - Reading query plans: Seq Scan vs Index Scan, cost estimates, actual time
    - Common slow query patterns: missing indexes, full table scans, unnecessary JOINs
    - Adding indexes with SQLAlchemy: `Index("ix_name", Column)` and Alembic migrations
    - Using SQLAlchemy events to log slow queries (`before_cursor_execute` / `after_cursor_execute`)
    - Key Takeaways

    **03-n-plus-one-queries.md**:
    - "Why This Matters" with mobile analogy (Core Data faulting / Room @Relation lazy loading vs SQLAlchemy lazy loading)
    - What N+1 is: 1 query for parent + N queries for children
    - Demonstrating the problem with SQLAlchemy (lazy="select" default)
    - Solution 1: `selectinload()` -- one extra SELECT with IN clause
    - Solution 2: `joinedload()` -- single JOIN query
    - Solution 3: `subqueryload()` -- subquery approach
    - When to use which: selectinload for collections, joinedload for single relations
    - Detecting N+1: SQLAlchemy echo, counting queries in tests
    - Key Takeaways

    **04-connection-pooling.md**:
    - "Why This Matters" with mobile analogy (URLSession connection reuse / OkHttp ConnectionPool vs SQLAlchemy engine pool)
    - What connection pooling is and why it matters
    - SQLAlchemy pool configuration: pool_size, max_overflow, pool_timeout, pool_recycle, pool_pre_ping
    - Pool types: QueuePool (default), NullPool (for testing), StaticPool (SQLite)
    - Monitoring pool usage: pool.status(), pool events
    - Common issues: pool exhaustion (too many concurrent requests), stale connections
    - Production settings for PostgreSQL with asyncpg
    - Key Takeaways

    **05-async-best-practices.md**:
    - "Why This Matters" with mobile analogy (GCD/async-await in Swift / Kotlin coroutines vs Python asyncio)
    - When async helps (I/O-bound) vs when it does not (CPU-bound)
    - FastAPI async vs sync endpoints: when to use `async def` vs `def`
    - Common mistake: blocking the event loop with sync code in async endpoints
    - Using `run_in_executor` for CPU-bound work
    - asyncio.gather for concurrent I/O
    - Async database sessions with SQLAlchemy async engine
    - Comparison table: sync vs async FastAPI endpoint performance characteristics
    - Key Takeaways

    **06-load-testing.md**:
    - "Why This Matters" with mobile analogy (XCTest performance tests / Android Benchmark library vs Locust/k6)
    - What load testing validates (throughput, latency percentiles, error rate under load)
    - Locust introduction: locustfile.py structure, @task decorator, HttpUser
    - Writing a locustfile for a FastAPI app
    - Running Locust: CLI and web UI
    - Interpreting results: RPS, response time percentiles (p50, p95, p99), failure rate
    - Simple benchmarking with `timeit` and `time.perf_counter` for micro-benchmarks
    - Load testing in CI (headless mode with `--headless`)
    - Key Takeaways
  </action>
  <verify>
    <automated>test -f 020-performance-optimization/README.md && ls 020-performance-optimization/theory/*.md | wc -l | grep -q "6" && echo "PASS: README + 6 theory files" || echo "FAIL"</automated>
  </verify>
  <done>Module 020 has a comprehensive README (100+ lines with mobile context table, prerequisites, time estimates) and 6 theory files (01-06), each with "Why This Matters" mobile analogies, code examples, comparison tables, and "Key Takeaways" sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create Module 020 exercises and project</name>
  <files>
    020-performance-optimization/exercises/01_profile_code.py
    020-performance-optimization/exercises/02_fix_n_plus_one.py
    020-performance-optimization/exercises/03_load_test.py
    020-performance-optimization/project/README.md
  </files>
  <action>
    Read 2-3 exercise files from completed modules (e.g., Module 021 or Module 007) for style reference. Follow the established exercise pattern exactly: docstring with instructions and mobile analogy, TODO stubs with hints, embedded pytest tests using `pass` placeholders.

    **01_profile_code.py** (~120-180 lines):
    - Docstring: Profile Python code to find the bottleneck
    - Mobile analogy: Like using Instruments Time Profiler to find the slow method in your iOS app
    - Run command: `pytest 020-performance-optimization/exercises/01_profile_code.py -v`
    - Provide a pre-built `slow_function()` with an intentional O(n^2) bottleneck (e.g., nested loops, repeated list concatenation, or repeated `sum(range(i))`)
    - TODO 1: Implement `find_bottleneck() -> str` that uses cProfile to profile `slow_function` and returns the name of the function that takes the most cumulative time (use `pstats.Stats` with `sort_stats("cumulative")`)
    - TODO 2: Implement `optimize_function() -> list` that reimplements `slow_function` more efficiently (e.g., using itertools.accumulate or a running sum)
    - TODO 3: Implement `compare_performance() -> dict` that times both functions and returns `{"slow": float, "fast": float}` using `time.perf_counter`
    - Tests: `find_bottleneck()` returns expected function name, `optimize_function()` produces same result as `slow_function()`, `compare_performance()["fast"] < compare_performance()["slow"]`

    **02_fix_n_plus_one.py** (~150-200 lines):
    - Docstring: Fix N+1 query problems using SQLAlchemy eager loading
    - Mobile analogy: Like optimizing Core Data fetch requests with prefetching, or Room @Transaction with @Relation
    - Run command: `pytest 020-performance-optimization/exercises/02_fix_n_plus_one.py -v`
    - Provide pre-built models: Author (id, name) and Book (id, title, author_id) using sync SQLAlchemy 2.0 with DeclarativeBase and SQLite in-memory (matching Module 007 pattern)
    - Provide a pre-built `setup_test_data(session)` function that creates 5 authors with 3 books each
    - Provide a pre-built `get_authors_n_plus_one(session) -> list[dict]` that demonstrates the N+1 problem (queries authors, then loops to access .books)
    - TODO 1: Implement `get_authors_with_books(session) -> list[dict]` using `selectinload` to fetch authors with books in 2 queries
    - TODO 2: Implement `get_authors_with_books_joined(session) -> list[dict]` using `joinedload` for single-query approach
    - TODO 3: Implement `count_queries(session, func) -> int` that counts the number of SQL queries executed by a function (use SQLAlchemy events: `before_cursor_execute` counter)
    - Tests: both eager-loading functions return same data as N+1 version, `count_queries` for eager-loaded versions returns <= 2 (vs N+1 which returns > 5), results include all 5 authors with 3 books each

    **03_load_test.py** (~100-150 lines):
    - Docstring: Write benchmarking and load test configuration
    - Mobile analogy: Like XCTest measure blocks or Android Benchmark library, but for HTTP endpoints
    - Run command: `pytest 020-performance-optimization/exercises/03_load_test.py -v`
    - Since Locust requires a running server, this exercise focuses on: (1) writing a locustfile as Python classes and (2) benchmarking pure functions
    - TODO 1: Implement `create_locust_user_class() -> type` that dynamically creates a class inheriting from a base with `tasks` attribute containing task functions (test validates class structure, NOT actual Locust execution -- do not import locust)
    - Alternative approach if Locust import is problematic: Define the locustfile structure as a dict (similar to CI/CD exercise pattern), OR focus entirely on micro-benchmarking
    - TODO 2: Implement `benchmark_function(func, iterations=100) -> dict` that runs a function N times and returns `{"min": float, "max": float, "avg": float, "p95": float}` using `time.perf_counter` and `statistics` module
    - TODO 3: Implement `compare_implementations(func_a, func_b, iterations=100) -> str` that benchmarks both and returns "a" or "b" for whichever is faster by average time
    - Tests: `benchmark_function` returns dict with correct keys and min <= avg <= max, `compare_implementations` correctly identifies the faster function when given one O(n) and one O(n^2) implementation, p95 >= avg

    **project/README.md** (~100-150 lines):
    - Follow established project README pattern
    - Title: "Project: Profile and Optimize a Slow API"
    - Overview: Given a deliberately slow FastAPI application, profile it, identify bottlenecks, and optimize
    - Requirements sections: (1) Profiling -- profile endpoints with cProfile, identify top 3 bottlenecks; (2) Query Optimization -- fix N+1 queries with eager loading, add database indexes; (3) Connection Pooling -- configure SQLAlchemy pool settings for production; (4) Async Optimization -- convert blocking endpoints to async where appropriate; (5) Benchmarking -- measure before/after performance with timing
    - Starter Template: FastAPI app structure with deliberately slow endpoints (N+1 queries, blocking sync calls, missing indexes, no connection pool config)
    - Success Criteria: 5-6 numbered verifiable outcomes (e.g., "All N+1 queries eliminated", "Response time for /users endpoint reduced by 50%+")
    - Stretch Goals: Load testing with Locust, memory profiling, database query logging middleware
  </action>
  <verify>
    <automated>ls 020-performance-optimization/exercises/*.py | wc -l | grep -q "3" && ls 020-performance-optimization/project/README.md > /dev/null 2>&1 && pytest 020-performance-optimization/exercises/ -v --co 2>&1 | grep -q "test session starts" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Module 020 has 3 exercise Python files with TODO stubs and embedded pytest tests that collect without import errors, plus a project README with requirements, starter template, and success criteria. Running `pytest --co` on the exercises directory shows test collection succeeds.</done>
</task>

</tasks>

<verification>
Module 020 content verification:
1. `wc -l 020-performance-optimization/README.md` returns 100+ lines (not the 63-line stub)
2. `ls 020-performance-optimization/theory/*.md | wc -l` returns 6
3. `ls 020-performance-optimization/exercises/*.py | wc -l` returns 3
4. `ls 020-performance-optimization/project/README.md` exists
5. `pytest 020-performance-optimization/exercises/ -v --co` collects tests without import errors
6. Each theory file contains "Why This Matters" and "Key Takeaways" sections
7. Each exercise file contains "TODO" stubs and embedded test classes
8. README.md has "Mobile Developer Context" comparison table
</verification>

<success_criteria>
Module 020 is complete with comprehensive README (rewritten from stub), 6 theory files, 3 exercises, and 1 project README. All content follows established patterns. Exercises use SQLite in-memory for database exercises and pure Python for profiling/benchmarking. All tests collect successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/09-complete-missing-content/09-02-SUMMARY.md`
</output>
