---
phase: 02-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 007-sqlalchemy-orm/theory/01-orm-concepts.md
  - 007-sqlalchemy-orm/theory/02-defining-models.md
  - 007-sqlalchemy-orm/theory/03-relationships.md
  - 007-sqlalchemy-orm/theory/04-crud-with-session.md
  - 007-sqlalchemy-orm/theory/05-async-sqlalchemy.md
  - 007-sqlalchemy-orm/theory/06-alembic-migrations.md
  - 007-sqlalchemy-orm/exercises/01_models_relationships.py
  - 007-sqlalchemy-orm/exercises/02_crud_operations.py
  - 007-sqlalchemy-orm/exercises/03_complex_queries.py
  - 007-sqlalchemy-orm/project/README.md
  - 008-api-crud-operations/theory/01-crud-endpoint-design.md
  - 008-api-crud-operations/theory/02-depends-db-sessions.md
  - 008-api-crud-operations/theory/03-service-layer.md
  - 008-api-crud-operations/theory/04-error-handling.md
  - 008-api-crud-operations/theory/05-pagination-filtering.md
  - 008-api-crud-operations/theory/06-bulk-operations.md
  - 008-api-crud-operations/exercises/01_crud_endpoints.py
  - 008-api-crud-operations/exercises/02_service_pattern.py
  - 008-api-crud-operations/exercises/03_pagination.py
  - 008-api-crud-operations/project/README.md
autonomous: true

must_haves:
  truths:
    - "Module 007 theory covers ORM concepts, model definitions, relationships, CRUD with session, async SQLAlchemy, and Alembic migrations in 6 files"
    - "Module 007 exercises provide TODO stubs for creating models with relationships, performing CRUD operations, and writing complex queries with inline pytest tests"
    - "Module 007 project README defines a task management data layer project with requirements, starter template, and success criteria"
    - "Module 008 theory covers CRUD endpoint design, Depends() for DB sessions, service layer pattern, error handling, pagination/filtering, and bulk operations in 6 files"
    - "Module 008 exercises provide TODO stubs for CRUD endpoints, service pattern implementation, and pagination with inline pytest tests"
    - "Module 008 project README defines a notes application CRUD API project with requirements, starter template, and success criteria"
    - "All theory files include Why This Matters mobile-dev framing and Key Takeaways"
    - "All code uses SQLAlchemy 2.0 patterns (Mapped, mapped_column, DeclarativeBase) not 1.x patterns"
    - "All code uses Pydantic v2 patterns (field_validator, model_config, model_dump) not v1"
    - "Exercise stubs use pass or minimal code so tests fail until learner implements"
  artifacts:
    - path: "007-sqlalchemy-orm/theory/02-defining-models.md"
      provides: "SQLAlchemy 2.0 model definitions with Mapped and mapped_column"
      contains: "Mapped"
    - path: "007-sqlalchemy-orm/theory/05-async-sqlalchemy.md"
      provides: "Async engine, session, and get_db dependency pattern"
      contains: "create_async_engine"
    - path: "007-sqlalchemy-orm/theory/06-alembic-migrations.md"
      provides: "Alembic setup, autogenerate, upgrade/downgrade workflow"
      contains: "alembic"
    - path: "007-sqlalchemy-orm/exercises/01_models_relationships.py"
      provides: "Exercise defining SQLAlchemy models with relationships"
      contains: "TESTS"
    - path: "007-sqlalchemy-orm/project/README.md"
      provides: "Task management data layer project"
      contains: "Success Criteria"
    - path: "008-api-crud-operations/theory/03-service-layer.md"
      provides: "Service layer pattern with dependency injection"
      contains: "Depends"
    - path: "008-api-crud-operations/theory/05-pagination-filtering.md"
      provides: "Pagination and filtering patterns for list endpoints"
      contains: "offset"
    - path: "008-api-crud-operations/exercises/01_crud_endpoints.py"
      provides: "Exercise building CRUD endpoints with FastAPI"
      contains: "TESTS"
    - path: "008-api-crud-operations/exercises/02_service_pattern.py"
      provides: "Exercise implementing service layer pattern"
      contains: "TESTS"
    - path: "008-api-crud-operations/project/README.md"
      provides: "Notes application CRUD API project"
      contains: "Success Criteria"
  key_links:
    - from: "007 theory (models, relationships)"
      to: "007 exercises (models, CRUD)"
      via: "Theory teaches model definitions and session operations, exercises apply them"
      pattern: "Mapped|mapped_column|relationship"
    - from: "007 theory (async SQLAlchemy)"
      to: "008 theory (Depends for DB sessions)"
      via: "Module 007 teaches async session setup, Module 008 uses it via Depends(get_db)"
      pattern: "get_db|AsyncSession|Depends"
    - from: "008 theory (service layer, CRUD design)"
      to: "008 exercises (service pattern, CRUD endpoints)"
      via: "Theory teaches service layer and endpoint design, exercises implement them"
      pattern: "Service|Depends|router"
---

<objective>
Create complete learning content for Module 007 (SQLAlchemy ORM) and Module 008 (Building CRUD APIs).

Purpose: Module 007 transitions from raw SQL to Python ORM — the key abstraction mobile developers need for backend work. Module 008 ties everything together: FastAPI + Pydantic + SQLAlchemy into production-ready CRUD APIs with proper architecture (service layer, dependency injection, pagination). Together they complete the "I can build a data-backed API" milestone.

Output: 6 theory files, 3 exercise files, and 1 project README for each module (20 files total).
</objective>

<execution_context>
@/Users/mbratkowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbratkowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-RESEARCH.md

# Reference patterns (read these first to match style)
@001-python-backend-quickstart/theory/01-environment.md
@001-python-backend-quickstart/exercises/01_types.py
@001-python-backend-quickstart/project/README.md

# Module READMEs (contain topic outlines to follow)
@007-sqlalchemy-orm/README.md
@008-api-crud-operations/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Module 007 — SQLAlchemy ORM content</name>
  <files>
    007-sqlalchemy-orm/theory/01-orm-concepts.md
    007-sqlalchemy-orm/theory/02-defining-models.md
    007-sqlalchemy-orm/theory/03-relationships.md
    007-sqlalchemy-orm/theory/04-crud-with-session.md
    007-sqlalchemy-orm/theory/05-async-sqlalchemy.md
    007-sqlalchemy-orm/theory/06-alembic-migrations.md
    007-sqlalchemy-orm/exercises/01_models_relationships.py
    007-sqlalchemy-orm/exercises/02_crud_operations.py
    007-sqlalchemy-orm/exercises/03_complex_queries.py
    007-sqlalchemy-orm/project/README.md
  </files>
  <action>
    Create all Module 007 content. This module introduces SQLAlchemy 2.0 ORM — the bridge from SQL to Python objects. Follow Module 001 patterns.

    CRITICAL: Use ONLY SQLAlchemy 2.0 patterns throughout:
    - Use `DeclarativeBase` NOT `declarative_base()`
    - Use `Mapped[type]` and `mapped_column()` NOT `Column()`
    - Use `Mapped[list["Model"]]` NOT `List["Model"]` from typing
    - Use `create_async_engine` and `AsyncSession` for async examples
    - Use `select()` NOT `session.query()`
    - Use `async_sessionmaker` NOT `sessionmaker` for async
    - Use `selectinload()` for eager loading examples

    **Theory files (6 files):**
    Each follows: `# Title` → `## Why This Matters` (mobile-dev analogy) → core content with code examples → `## Key Takeaways`.

    1. `01-orm-concepts.md` — What is an ORM and why use one. Cover: mapping database tables to Python classes, mapping rows to objects, mapping columns to attributes. Benefits: type safety, no SQL injection, database-agnostic code, migration support. Tradeoffs: performance overhead, abstraction leaks (N+1 queries). Analogy: "An ORM is like Core Data or Room — you define entity classes and the framework handles the SQL. SQLAlchemy is the most powerful ORM in Python." Compare: Core Data NSManagedObject = SQLAlchemy model, Room @Entity = SQLAlchemy declarative model, Core Data stack = SQLAlchemy engine+session, Room queries = SQLAlchemy select().

    2. `02-defining-models.md` — Creating SQLAlchemy 2.0 models. Cover: inheriting from DeclarativeBase, `__tablename__`, field types with `Mapped[type]` and `mapped_column()`, primary keys, String length constraints, nullable fields (`Mapped[str | None]`), default values, indexes, unique constraints. Show complete model: User with id, username, email, created_at, is_active. Analogy: "Defining a model is like creating a Room @Entity or Core Data entity in the model editor — but in pure Python code with type hints." Show the mapping between SQL CREATE TABLE and the Python model.

    3. `03-relationships.md` — SQLAlchemy relationships. Cover: `relationship()` for object references, `ForeignKey` for database column, `back_populates` for bidirectional relationships. One-to-many: User → Posts. One-to-one: User → Profile. Many-to-many: Students ↔ Courses with association table. Eager loading with `selectinload()` and `joinedload()`. The N+1 query problem and how to prevent it. `cascade="all, delete-orphan"` for cleanup. Analogy: "Relationships are like @Relation in Room or NSRelationship in Core Data — they let you navigate between connected objects." Show the N+1 problem with echo=True to see generated SQL, then fix with selectinload().

    4. `04-crud-with-session.md` — CRUD operations using SQLAlchemy session. Cover: Create (session.add + commit + refresh), Read (select().where(), scalar_one_or_none(), scalars().all()), Update (modify attributes + commit), Delete (session.delete + commit). Transaction management: session.begin(), commit, rollback. Flushing vs committing. Analogy: "The session is like a managed object context in Core Data or a database transaction in Room — it tracks changes and writes them to the database when you commit." Show all CRUD operations with the User model.

    5. `05-async-sqlalchemy.md` — Async SQLAlchemy setup for FastAPI. Cover: `create_async_engine()` with `postgresql+asyncpg://` URL, `async_sessionmaker` configuration (expire_on_commit=False, autoflush=False), the `get_db()` dependency function using `async with` and `yield`. Why expire_on_commit=False is critical in async. Connection pool settings (pool_size, max_overflow, pool_pre_ping). Analogy: "Async database access is like using Combine/Flow for network calls — the database operation runs without blocking the event loop, so your server can handle other requests." Show the complete database setup file that Module 008 will use.

    6. `06-alembic-migrations.md` — Database migrations with Alembic. Cover: Why migrations (version-controlled schema changes, team collaboration, rollback capability). Setup: `alembic init`, configuring env.py to import models and set target_metadata, setting database URL. Workflow: `alembic revision --autogenerate -m "description"`, reviewing generated migration, `alembic upgrade head`, `alembic downgrade -1`, `alembic history`. Show a real migration file (upgrade/downgrade functions). When to use `create_all()` vs Alembic (create_all for tests only, Alembic for everything else). Analogy: "Migrations are like database versioning — just as you version your app (1.0, 1.1, 2.0), you version your database schema. Each migration is a step that can be applied or rolled back."

    **Exercise files (3 files):**
    Module 007 exercises work with SQLAlchemy models and sessions. Since we can't require a running database for exercises, use SQLAlchemy with an in-memory SQLite database for tests (`sqlite+aiosqlite://` or `sqlite://` sync for simplicity). Define models in the exercise file, create tables with `Base.metadata.create_all()`, and test model behavior. Use sync SQLAlchemy for exercises to keep tests simple (async testing adds complexity — covered in Module 012).

    1. `01_models_relationships.py` — Define SQLAlchemy models with relationships:
       - Provide `Base = DeclarativeBase()` at top of file
       - TODO: Create `Author` model: id (int, PK), name (str, max 100, not null), email (str, max 255, unique), bio (str | None). Relationship: `books: Mapped[list["Book"]]` with back_populates.
       - TODO: Create `Book` model: id (int, PK), title (str, max 200, not null), isbn (str, max 13, unique), published_year (int | None), author_id (int, FK to authors.id). Relationship: `author: Mapped["Author"]` with back_populates.
       - TODO: Create `Tag` model: id (int, PK), name (str, max 50, unique). Relationship: books (many-to-many).
       - TODO: Create `book_tags` association table for Book ↔ Tag many-to-many.
       Tests use sync SQLite in-memory engine: create tables, create Author+Book, verify relationship navigation, test many-to-many through association. Verify: `author.books` returns list, `book.author` returns Author, `book.tags` returns tags. Use `from sqlalchemy import create_engine` and `from sqlalchemy.orm import Session`.
       Header: `Run: pytest 007-sqlalchemy-orm/exercises/01_models_relationships.py -v`

    2. `02_crud_operations.py` — Perform CRUD with session:
       - Provide Author and Book models (completed, not stubs) and SQLite engine setup.
       - TODO: `create_author(session: Session, name: str, email: str) -> Author` — Create and return new author.
       - TODO: `get_author_by_id(session: Session, author_id: int) -> Author | None` — Get author by ID or None.
       - TODO: `get_authors_by_name(session: Session, name_pattern: str) -> list[Author]` — Search authors where name contains pattern (case-insensitive). Use `ilike()` for matching.
       - TODO: `update_author_email(session: Session, author_id: int, new_email: str) -> Author | None` — Update author's email, return updated author or None if not found.
       - TODO: `delete_author(session: Session, author_id: int) -> bool` — Delete author, return True if deleted, False if not found.
       - TODO: `create_book_for_author(session: Session, author_id: int, title: str, isbn: str) -> Book | None` — Create book for author, return None if author not found.
       Tests: create author, verify retrieval, update email, search by name, delete, create book linked to author.
       Header: `Run: pytest 007-sqlalchemy-orm/exercises/02_crud_operations.py -v`

    3. `03_complex_queries.py` — Write complex queries:
       - Provide Author, Book, Tag models and seed data function (3 authors, 8 books, 5 tags with assignments).
       - TODO: `get_authors_with_book_count(session: Session) -> list[dict]` — Return authors with their book count, sorted by count descending. Use `func.count()` and `group_by()`.
       - TODO: `get_books_by_year_range(session: Session, start_year: int, end_year: int) -> list[Book]` — Get books published between start_year and end_year inclusive.
       - TODO: `get_prolific_authors(session: Session, min_books: int) -> list[Author]` — Get authors with at least min_books books. Use HAVING clause.
       - TODO: `get_books_with_tags(session: Session, tag_names: list[str]) -> list[Book]` — Get books that have ALL specified tags. Use subquery or join approach.
       Tests verify query results against known seed data.
       Header: `Run: pytest 007-sqlalchemy-orm/exercises/03_complex_queries.py -v`

    **Project README:**
    `project/README.md` — "Task Management Data Layer." Overview: Build the data layer (models + CRUD operations) for a task management application. Requirements:
    1. Define models: User (id, username, email), Project (id, name, description, owner_id FK), Task (id, title, description, status enum [todo/in_progress/done], priority int 1-5, project_id FK, assignee_id FK, created_at, updated_at), Comment (id, content, task_id FK, author_id FK, created_at)
    2. Set up SQLite database with create_all (for this exercise)
    3. Implement CRUD functions: create_project, list_user_projects, create_task, update_task_status, assign_task, get_project_tasks (with status filter), add_comment, get_task_comments
    4. Write queries: tasks by status per project, overdue tasks, user's assigned tasks across projects

    Starter template: Python file with model stubs, engine setup, and function stubs. Success criteria checkboxes. Stretch goals: add task labels (many-to-many), add task history tracking (status changes), add project statistics query.

    IMPORTANT:
    - Use SQLAlchemy 2.0 patterns ONLY (Mapped, mapped_column, DeclarativeBase)
    - Use sync SQLAlchemy with SQLite for exercises (keeps testing simple)
    - Stubs should use `pass` so tests FAIL
    - Import from `sqlalchemy` and `sqlalchemy.orm`, NOT `sqlalchemy.ext.asyncio` in exercises
    - Match Module 001 exercise and project style exactly
  </action>
  <verify>
    All 10 files exist:
    - `ls 007-sqlalchemy-orm/theory/0{1,2,3,4,5,6}-*.md | wc -l` = 6
    - `ls 007-sqlalchemy-orm/exercises/0{1,2,3}_*.py | wc -l` = 3
    - `ls 007-sqlalchemy-orm/project/README.md`
    - Each theory file contains "## Why This Matters" and "## Key Takeaways"
    - Each exercise file contains "# ============= TESTS ============="
    - Theory files use SQLAlchemy 2.0 patterns: `grep -r "Mapped" 007-sqlalchemy-orm/theory/` has matches
    - No v1 patterns: `grep -r "declarative_base()" 007-sqlalchemy-orm/` returns nothing
    - No v1 patterns: `grep -r "Column(" 007-sqlalchemy-orm/theory/` returns nothing (Column OK in migration examples only)
    - `pytest 007-sqlalchemy-orm/exercises/ -v` runs (tests fail because stubs use pass)
    - Project README contains "## Success Criteria" and "## Starter Template"
  </verify>
  <done>
    Module 007 has 6 theory files covering ORM concepts through Alembic migrations, 3 exercise files with TODO stubs and SQLite-backed inline pytest tests, and 1 project README for a task management data layer. All files use SQLAlchemy 2.0 patterns (Mapped, mapped_column, DeclarativeBase) and follow established content patterns with mobile-dev framing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Module 008 — Building CRUD APIs content</name>
  <files>
    008-api-crud-operations/theory/01-crud-endpoint-design.md
    008-api-crud-operations/theory/02-depends-db-sessions.md
    008-api-crud-operations/theory/03-service-layer.md
    008-api-crud-operations/theory/04-error-handling.md
    008-api-crud-operations/theory/05-pagination-filtering.md
    008-api-crud-operations/theory/06-bulk-operations.md
    008-api-crud-operations/exercises/01_crud_endpoints.py
    008-api-crud-operations/exercises/02_service_pattern.py
    008-api-crud-operations/exercises/03_pagination.py
    008-api-crud-operations/project/README.md
  </files>
  <action>
    Create all Module 008 content. This module brings everything together: FastAPI + Pydantic + SQLAlchemy into complete CRUD APIs with proper architecture. Follow Module 001 patterns.

    CRITICAL patterns to enforce:
    - SQLAlchemy 2.0: Mapped, mapped_column, DeclarativeBase, select()
    - Pydantic v2: field_validator, model_config = ConfigDict(...), model_dump(), str | None
    - FastAPI: Depends(), async def handlers, status codes, HTTPException
    - Service layer: thin routes that delegate to service classes

    **Theory files (6 files):**
    Each follows: `# Title` → `## Why This Matters` (mobile-dev analogy) → core content with code examples → `## Key Takeaways`.

    1. `01-crud-endpoint-design.md` — Designing CRUD endpoints. Cover: the 5 standard CRUD endpoints (POST create, GET list, GET by id, PUT/PATCH update, DELETE), HTTP method and status code mapping (POST→201, GET→200, PUT/PATCH→200, DELETE→204), request/response schema design (separate Create, Update, Response schemas), URL patterns (plural nouns: /users, /users/{id}). Show complete CRUD router for a resource. Analogy: "This is like designing your mobile app's API client interface — but now you're the one defining the contract. Every endpoint you've called as a mobile dev, you're now building."

    2. `02-depends-db-sessions.md` — FastAPI Depends() for database sessions. Cover: What is dependency injection (Depends), the get_db() pattern (async generator yielding session), why yield (automatic cleanup), composing dependencies (get_db → get_service), overriding dependencies for testing. Show the complete flow: request → Depends(get_db) → session created → handler runs → session closed. Analogy: "Dependency injection in FastAPI is like @Inject in Dagger/Hilt or the Environment in SwiftUI — the framework provides what your function needs without you constructing it." Show how to override get_db in tests with a test database.

    3. `03-service-layer.md` — Service layer pattern. Cover: Why separate routes from business logic (testability, reusability, single responsibility), service class structure (receives session, provides CRUD methods), the route→service flow (route validates input, calls service, returns response). Show refactoring: before (logic in route handler) vs after (thin route + service class). Show how to create a service dependency: `def get_user_service(db: AsyncSession = Depends(get_db)) -> UserService`. Analogy: "The service layer is like your mobile app's Repository or ViewModel — it sits between the UI (routes) and data (database), handling business logic. Your route handler stays thin, like a ViewController that just calls the ViewModel."

    4. `04-error-handling.md` — Error handling in CRUD APIs. Cover: HTTPException for standard errors (404 Not Found, 400 Bad Request, 409 Conflict for duplicates), custom exception handlers with `@app.exception_handler()`, consistent error response format (`{"detail": "message"}`), handling database errors (IntegrityError for unique violations, wrapping in HTTPException), validation error customization. Analogy: "Error handling here is like your mobile app's error states — you map backend errors to user-friendly messages. Now you're the one creating those error codes." Show: not found (404), duplicate (409 from IntegrityError), validation error (422 auto from Pydantic).

    5. `05-pagination-filtering.md` — Pagination and filtering patterns. Cover: Offset-based pagination (skip/limit or page/page_size), response format with total count and page metadata, query parameter filters (category, status, date range), ordering (sort_by, sort_order), combining pagination with filters. Show: PaginatedResponse generic model, paginate helper function using SQLAlchemy select().offset().limit() + func.count(). Analogy: "Every mobile app with a list view needs pagination — infinite scroll or page numbers. Now you're building the API that powers that list." Show complete paginated list endpoint with filters.

    6. `06-bulk-operations.md` — Bulk create, update, and delete. Cover: bulk create (accept list of items, insert all in one transaction), bulk update (update multiple records by criteria), bulk delete (delete by IDs), transaction management for bulk ops (all-or-nothing), performance considerations (batching large operations). Analogy: "Bulk operations are like syncing offline changes in your mobile app — you queue up many changes and send them all at once." Show: POST /items/bulk accepting list, DELETE /items/bulk accepting list of IDs, batch processing pattern.

    **Exercise files (3 files):**
    Module 008 exercises use FastAPI TestClient with an in-memory SQLite database. Each exercise creates a complete mini-app (models, schemas, routes) for testing. Use sync SQLAlchemy with SQLite for test simplicity (matching Module 007 exercises).

    1. `01_crud_endpoints.py` — Build complete CRUD endpoints:
       - Provide: SQLAlchemy Base, Item model (id, name, description, price, in_stock bool), Pydantic schemas (ItemCreate, ItemUpdate, ItemResponse with ConfigDict(from_attributes=True)), SQLite engine setup, get_db dependency, FastAPI app.
       - TODO: POST `/items` — Create item, return 201 with created item
       - TODO: GET `/items` — List all items
       - TODO: GET `/items/{item_id}` — Get single item or 404
       - TODO: PUT `/items/{item_id}` — Update item (all fields required) or 404
       - TODO: PATCH `/items/{item_id}` — Partial update (only provided fields) or 404
       - TODO: DELETE `/items/{item_id}` — Delete item, return 204 or 404
       Tests use TestClient to verify all CRUD operations: create, read, list, update, partial update, delete, 404 on missing.
       Header: `Run: pytest 008-api-crud-operations/exercises/01_crud_endpoints.py -v`

    2. `02_service_pattern.py` — Implement the service layer pattern:
       - Provide: SQLAlchemy models (User with id, username, email), Pydantic schemas (UserCreate, UserUpdate, UserResponse), database setup, FastAPI app.
       - TODO: Create `UserService` class with methods:
         - `create_user(self, user_data: UserCreate) -> User`
         - `get_user(self, user_id: int) -> User | None`
         - `list_users(self, skip: int = 0, limit: int = 100) -> list[User]`
         - `update_user(self, user_id: int, user_data: UserUpdate) -> User | None`
         - `delete_user(self, user_id: int) -> bool`
       - TODO: Create `get_user_service` dependency that creates UserService with db session
       - TODO: Create route handlers that use `service: UserService = Depends(get_user_service)` — routes should be thin (validate input, call service, return response or raise HTTPException)
       Tests verify: service creates/reads/updates/deletes users through API, duplicate username returns 400, missing user returns 404, routes delegate to service (not doing DB work directly).
       Header: `Run: pytest 008-api-crud-operations/exercises/02_service_pattern.py -v`

    3. `03_pagination.py` — Implement pagination and filtering:
       - Provide: Product model (id, name, category, price, in_stock), ProductResponse schema, database with seed data (15 products across 3 categories, varying prices).
       - TODO: Create `PaginatedResponse` Pydantic model (items: list, total: int, page: int, page_size: int, pages: int)
       - TODO: GET `/products` with query params: `page: int = 1`, `page_size: int = 10`, `category: str | None = None`, `min_price: float | None = None`, `max_price: float | None = None`, `sort_by: str = "id"`, `sort_order: str = "asc"`
       - TODO: Implement the query building: apply filters, count total, apply sort, apply pagination offset/limit, return PaginatedResponse
       Tests verify: default pagination (page 1, size 10), custom page size, filtering by category, filtering by price range, sorting, correct total/pages calculation, empty results.
       Header: `Run: pytest 008-api-crud-operations/exercises/03_pagination.py -v`

    **Project README:**
    `project/README.md` — "Notes Application CRUD API." Overview: Build a complete CRUD API for a notes application with proper architecture. Requirements:
    1. Models: User (id, username, email), Notebook (id, title, description, owner_id FK, created_at), Note (id, title, content, notebook_id FK, is_pinned bool, created_at, updated_at)
    2. Pydantic schemas: Create, Update, Response for each model (use ConfigDict(from_attributes=True))
    3. Service layer: NotebookService, NoteService with all CRUD methods
    4. Endpoints:
       - POST/GET/GET:id/PUT/DELETE for /notebooks
       - POST/GET/GET:id/PUT/DELETE for /notebooks/{id}/notes (nested resource)
       - GET /notes with pagination and filters (search by title, filter by notebook, filter by pinned)
       - PATCH /notes/{id}/pin (toggle pin status)
    5. Error handling: 404 for missing resources, 400 for invalid input, 409 for duplicate titles within notebook
    6. Pagination: page/page_size with total count on list endpoints

    Starter template: Python file with model stubs, schema stubs, service stubs, and route stubs. Success criteria checkboxes. Stretch goals: add note tags (many-to-many), add full-text search on note content, add soft delete (archived_at timestamp instead of hard delete), add bulk note operations.

    IMPORTANT:
    - Use sync SQLAlchemy with SQLite for exercises (TestClient + sync session)
    - Use Pydantic v2 patterns ONLY (ConfigDict, field_validator, model_dump)
    - Use SQLAlchemy 2.0 patterns ONLY (Mapped, mapped_column, select())
    - Stubs use `pass` so tests FAIL
    - Route handlers must be thin — delegate to service layer
    - Match Module 001 exercise and project style exactly
  </action>
  <verify>
    All 10 files exist:
    - `ls 008-api-crud-operations/theory/0{1,2,3,4,5,6}-*.md | wc -l` = 6
    - `ls 008-api-crud-operations/exercises/0{1,2,3}_*.py | wc -l` = 3
    - `ls 008-api-crud-operations/project/README.md`
    - Each theory file contains "## Why This Matters" and "## Key Takeaways"
    - Each exercise file contains "# ============= TESTS ============="
    - Each exercise file contains "TestClient" and "Depends"
    - Theory uses SQLAlchemy 2.0: `grep -r "Mapped" 008-api-crud-operations/theory/` has matches
    - Theory uses Pydantic v2: `grep -r "ConfigDict" 008-api-crud-operations/theory/` has matches
    - No v1 patterns: `grep -r "@validator" 008-api-crud-operations/` returns nothing
    - `pytest 008-api-crud-operations/exercises/ -v` runs (tests fail because stubs use pass)
    - Project README contains "## Success Criteria" and "## Starter Template"
  </verify>
  <done>
    Module 008 has 6 theory files covering CRUD design through bulk operations, 3 exercise files with TODO stubs and TestClient-based inline pytest tests using service layer pattern and pagination, and 1 project README for a notes application CRUD API. All files use SQLAlchemy 2.0 and Pydantic v2 patterns, Depends() for dependency injection, and follow established content patterns with mobile-dev framing.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. File count: `find 007-sqlalchemy-orm 008-api-crud-operations -name "*.md" -o -name "*.py" | grep -v README.md$ | wc -l` + project READMEs should total 20 files (10 per module)
2. Theory pattern: Every theory .md file contains both "## Why This Matters" and "## Key Takeaways"
3. Exercise pattern: Every exercise .py file contains `# ============= TESTS =============` separator and `def test_` functions
4. SQLAlchemy 2.0: Theory and exercise files use Mapped, mapped_column, DeclarativeBase (not Column, declarative_base)
5. Pydantic v2: No file contains `@validator` or `class Config:` — only `@field_validator`, `ConfigDict`
6. Module 008 exercises: Use TestClient, Depends, service layer pattern
7. Project READMEs: Both contain "## Success Criteria" with checkbox items and "## Starter Template"
8. No solution code in exercises: Stubs use `pass` or return minimal placeholders
</verification>

<success_criteria>
- 20 new content files created (10 per module)
- All theory files have "Why This Matters" mobile-dev framing and "Key Takeaways"
- All exercise files have TODO stubs with inline pytest tests that fail until implemented
- Both project READMEs have requirements, starter templates, and success criteria
- Module 007 exercises use sync SQLAlchemy with SQLite for testability
- Module 008 exercises use TestClient + Depends + service layer pattern
- SQLAlchemy 2.0 patterns used exclusively (Mapped, mapped_column, DeclarativeBase)
- Pydantic v2 patterns used exclusively (field_validator, ConfigDict, model_dump)
- Content progresses logically: ORM concepts (007) → CRUD API architecture (008)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-02-SUMMARY.md`
</output>
